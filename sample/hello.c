#include <stdio.h>
#include <stdlib.h>

#define MAXFILE 65535

int main(void)
{
        int tmp = 3;        // tmp
        /* print 3 times */
        for (int i = 0; i < tmp; i++)
                printf("hello world.\n");
    return 0;
}