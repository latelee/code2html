基于LEX的用于HTML的语法高亮工具

///////////////////////////////////////////////////////////
编写目的：
为在网站、博客、论坛上显示自定义的语法高亮着色，使用LEX作词法
分析，将与HTML语言结合，可以在大部分网站上显示。——但也有部分
是不支持的。

放到网站上的代码及Makefile只是为了方便显示，可能与实际的使用有
出入，如Makefile中的命令必须是Tab开头，但网站上的已经被4个或8个
空格代替，因此需要将其改回Tab。

其它没有明确说明的注意事项不再说明。

//////////////////////////////////////////////////////////
三种语法高亮：
1、代码，主要是C/C++
2、makefile文件
3、shell脚本及命令行

////////////////////////////////////////////////////////////
编译及用法：

1、Linux环境：
条件：假设系统中已有lex
编译：$ make -f Makefile-linux
清除：$ make -f Makefile-linux clean

用法：
$ code2html < hello.c > hello.html

2、Windows环境：
条件：cl.exe必须在PATH中，当前目录必须有flex.exe(已有)
编译：DOS> nmake -f Makefile-win32
清除：DOS> nmake -f Makefile-win32 clean

用法：
DOS> code2html.exe < hello.c > hello.html

发布到网站上：
生成html文件后，双击打开，复制到博客或论坛的文章编辑器中相应位置即可。
示例：
在
http://blog.csdn.net/subfate
http://www.latelee.org
上大部分的代码都是用这种方法生成的语法着色的。

//////////////////////////////////////////////////////////
文件列表：
sample：一些生成的例子
code2html.lex cofig2html.lex make2html.lex：对应的lex源代码，由于编写
时间间隔比较长，可能其中的某些部分没有及时更新。
Makefile-linux Makefile-win32：对应两个系统的Makefile文件
flex.exe：在网上下载的windows版本的lex，在dos下可直接运行。
start.bat：双击进入windows的dos命令行
readme.txt：本文件。

////////////////////////////////////////////////////////
已知bug:
1、不能处理连续的Tab，暂时的解决方法是使用文本编辑器将tab替换为4个或8个
空格
2、头文件中的“.”，如stdio.h，会被识别为数字的小数点。

///////////////////////////////////////////////////////
未知bug
其它未知bug，有待进一步发现

//////////////////////////////////////////////////////
免责说明：
本程序最初的想法来自《纯C论坛·电子杂志》中的一篇语法高亮文章，
作者是tinyfool，不过的源代码已经无法找到，我是根据这篇文章的代码
以及ChinaUnix论坛上的代码，并根据自己的习惯及使用场合修改而成(注)。

因此，在非商业情况下，代码可任意使用而不用通知作者，但作者不提供
技术支持。另外，对程序进行的修改，可能不会及时更新，而且，也可能不再
更新。

注：作者在学习《编译原理》时接触过LEX，但当时没深入学习，没利用LEX做
出一个像样的东西，后来建立了个人主页时，又稍微学习了HTML，因此，两者
的结合，对于作者来说，不算新鲜东西，也不算很难的东西。但知识这东西，
时间一久便会忘了——这个程序的初次编写至今已有半年了。